enum PanelContents {
  aboutMe,
  projects,
  experience,
}

enum ButtonStatus {
  notSelected,
  selected,
  hovered,
}

enum Skills {
  javaScript,
  flutter,
  dart,
  typeScript,
  nodeJs,
  nestJs,
  firebase,
  db2,
}

enum ThemeModeEnum {
  dark,
  light,
}

enum ProjectType {
  mobile,
  web,
}
